{
  "name": "@humanly/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Humanly",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "test": "pnpm build && node dist/config/edition.test.js && node dist/utils/event-metadata.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
